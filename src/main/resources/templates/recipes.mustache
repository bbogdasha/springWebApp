<!DOCTYPE HTML>
<html>
<head>
    <title>Recipes list</title>
    <style type="text/css">
        .main {
            max-width: 1140px;
            margin-right: auto;
            margin-left: auto;

        }
        .container {
            background: #bbb;
	        display: flex;
	        flex-wrap: wrap;
	        justify-content: space-between;
        }
        .box {
            width: 32%;
            height: 400px;
            margin: 1% 0 1% 0;
        }
        .box_picture {
            width: 100%;
            height: 300px;
            background: blue;
        }
        .box_name {
            width: 100%;
            height: 100px;
            background: red;
        }
    </style>
</head>
<body>
    <form action="/logout" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
        <input type="submit" value="Sign Out"/>
    </form>

    <div class="main">
        <a href="/createRecipe">Create new recipe</a><br>

        <h3>Recipes list:</h3>
        <div class="container">
            {{#recipes}}
                <div class="box">
                    <div class="box_picture">
                        Picture
                    </div>
                    <div class="box_name">
                        <span>Recipe name: {{name}}</span>
                        <span>Cooking time: {{time}}</span>
                    </div>
                </div>
            {{/recipes}}
        </div>
    </div>
</body>
</html>